<!DOCTYPE html>
<html>
  <head>
    <title>Go Fish</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
  </head>
  <body>
    @using GoFish.Models;
    <br>
    <h2>Number of cards left in the deck: @Model.NewDeck.DeckOfCards.Count</h2>
    <h2>Cards left in Computer Hand: @Model.ComputerObject.Hand.Count</h2>
    <br>
    <h2>Your books: @Model.PlayerObject.BooksCount</h2>
    <h2>Computers books: @Model.ComputerObject.BooksCount</h2>
    <br>
    <h1>Your Hand</h1>
    @foreach (Card card in Model.PlayerObject.Hand)
    {
      <form action="/games/ask-card" method="post">
        <input name="cardValue" type="text" value="@card.Value" hidden>
        <input name="cardSuit" type="text" value="@card.Suit" hidden>
        <button type="submit" name="button">@card.Value of @card.Suit</button>
      </form>
    }
  </body>
  </html>